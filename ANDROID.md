# تطبيق أندرويد (APK) — يومياتي

## أولاً: صراحة تقنية

**لم أستطع بناء ملف APK داخل هذه الجلسة.** بناء تطبيق أندرويد يتطلّب تحميل
Android SDK وGradle من خوادم Google، وهذه المنافذ محجوبة في البيئة التي أعمل
فيها (تُرجع 403). أي ملف «APK» أقدّمه لك دون بناء حقيقي سيكون ملفاً معطوباً
لا يُثبَّت — وهذا أسوأ من لا شيء.

لذلك أعطيك ثلاثة طرق مضمونة، مرتّبة من الأسهل، مع ملفات الإعداد جاهزة.

---

## الطريق ١ — التثبيت المباشر (بلا APK، دقيقتان)

هذا يكفي أغلب الحاجة، وهو ما يفعله الناس فعلاً مع تطبيقات الويب:

1. انشر المجلّد على GitHub Pages (خطوات README).
2. افتح الرابط بمتصفّح **كروم** على هاتف أندرويد.
3. القائمة (⋮) ← **«تثبيت التطبيق»** أو «إضافة إلى الشاشة الرئيسية».

النتيجة: أيقونة على شاشتك، يفتح بملء الشاشة بلا شريط متصفّح، يعمل بلا
إنترنت، وتصله الإشعارات. الفرق الوحيد عن APK أنه غير قابل للنشر على متجر
Google Play.

---

## الطريق ٢ — PWABuilder (APK موقّع، خمس دقائق، بلا أدوات)

1. انشر التطبيق أولاً (لا بدّ من رابط HTTPS).
2. افتح **pwabuilder.com** وألصق رابطك.
3. اضغط **Package for stores ← Android ← Generate**.
4. نزّل الحزمة: فيها `app-release-signed.apk` للتثبيت المباشر،
   و`app-release-bundle.aab` لمتجر Play، وملف `assetlinks.json`.
5. انسخ `assetlinks.json` إلى مجلّد `.well-known/` في مستودعك
   (المسار النهائي: `https://<حسابك>.github.io/.well-known/assetlinks.json`)
   ثم ارفعه. بدون هذه الخطوة سيظهر شريط عنوان المتصفّح داخل التطبيق.
6. انقل الـ APK إلى هاتفك وثبّته (فعّل «تثبيت من مصادر غير معروفة»).

> ملاحظة: على GitHub Pages لا بدّ أن يكون `.well-known` في **جذر النطاق**،
> أي في مستودع باسم `<حسابك>.github.io` لا داخل مستودع المشروع.

---

## الطريق ٣ — Bubblewrap على جهازك (تحكّم كامل)

ملف `twa-manifest.json` المرفق جاهز ومضبوط على حسابك — عدّل الروابط إن
اختلف اسم المستودع.

```bash
npm install -g @bubblewrap/cli
mkdir yawmiyati-android && cd yawmiyati-android
cp /المسار/twa-manifest.json .
bubblewrap init --manifest=https://mkhald357.github.io/yawmiyati/manifest.json
bubblewrap build          # ينزّل JDK وAndroid SDK أول مرة (~٢ غيغابايت)
```

المخرجات: `app-release-signed.apk` و`app-release-bundle.aab`.
استخرج بصمة التوقيع لملف assetlinks:

```bash
keytool -list -v -keystore android.keystore -alias yawmiyati
```

انسخ قيمة **SHA256** وضعها مكان `ضع_هنا_بصمة_مفتاح_التوقيع` في
`wellknown/assetlinks.json`، ثم ارفع الملف إلى `.well-known/` في جذر نطاقك.

---

## ماذا يتغيّر في نسخة APK؟

| | تثبيت من كروم | APK |
|---|---|---|
| أيقونة وشاشة كاملة | ✔ | ✔ |
| عمل بلا إنترنت | ✔ | ✔ |
| إشعارات ومنبّهات | ✔ | ✔ |
| مشاركة الملف مع أحد | ✖ | ✔ |
| رفعه على Google Play | ✖ | ✔ |

المحرّك واحد في الحالتين: الـ APK غلاف رقيق (Trusted Web Activity) حول نفس
الصفحة. لا فرق في السلوك ولا في المنبّه.

## ملفات هذا المجلّد

- `twa-manifest.json` — إعداد البناء جاهزاً (Bubblewrap)
- `wellknown/assetlinks.json` — قالب ربط التطبيق بنطاقك، ينقصه سطر البصمة
