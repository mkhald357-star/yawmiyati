# تطبيق أندرويد المستقل والمحلي بالكامل (Native Offline Android App) — مدبر المهام

تم تحويل المشروع بالكامل إلى **تطبيق أندرويد أصيل (Native Android Application)** مستقل 100% يعمل محلياً دون الحاجة لأي خادم أو اتصال بالإنترنت أو الاعتماد على TWA أو متصفح خارجي.

---

## 🌟 مميزات تطبيق الأندرويد الأصيل

1. **مستقل ومحلي بالكامل (100% Offline):**
   - جميع ملفات الواجهة والمنطق وقواعد البيانات المحلية والصوتيات مدمجة مباشرة داخل حزمة الـ APK (`assets/www/`).
   - لا يحتاج خوادم خارجية ولا يتطلب نشر على GitHub Pages ليعمل.
   - لا يحتاج إلى ملفات تحقق خارجية (`assetlinks.json`).

2. **بيئة تشغيل أندرويد متكاملة (AndroidX WebKit):**
   - استخدام `WebViewAssetLoader` من مكتبة Google الرسمية لتحميل الأصول المحلية بأعلى درجات الأمان والسرعة.
   - دعم كامل وتلقائي لقواعد البيانات المحلية `IndexedDB` و `localStorage`.
   - دعم تشغيل الصوت التلقائي للمنبهات بدون قيود المتصفحات التقليدية.

3. **تكامل كامل مع نظام الأندرويد:**
   - **زر الرجوع الفيزيائي/النظامي:** إغلاق النوافذ المنبثقة والعودة للرئيسية بسلاسة بدلاً من إغلاق التطبيق فجأة.
   - **شريط الحالة وشريط التنقل:** تناسق ألوان الشريط العلوي والسفلي مع ألوان التطبيق في الوضع الفاتح والداكن.
   - **الاختصارات السريعة (App Shortcuts):** الضغط المطول على أيقونة التطبيق لفتح "إضافة حركة"، "المهام"، "المنبه" مباشرة.
   - **أذونات الإشعارات والاهتزاز:** دعم أذونات أندرويد 13+ (`POST_NOTIFICATIONS` و `VIBRATE`).

---

## 📦 كيفية بناء ملفات APK و AAB

### الطريقة ١: البناء التلقائي عبر GitHub Actions (الأسهل والأسرع)
1. ارفع المشروع إلى مستودع GitHub الخاص بك.
2. توجّه إلى تبويب **Actions** في مستودعك.
3. اختر workflow **«بناء تطبيق أندرويد (Native APK & AAB)»** واضغط **Run workflow**.
4. بعد اكتمال البناء خلال دقائق، ستجد ملفات `APK` للتثبيت الفوري و `AAB` للنشر على متجر Google Play متاحة للتنزيل مباشرة من قسم Artifacts.

### الطريقة ٢: البناء المحلي باستخدام Android Studio أو Gradle
1. افتح مجلد `android` باستخدام **Android Studio**.
2. أو من سطر الأوامر (PowerShell / Terminal):
   ```bash
   cd android
   ./gradlew assembleRelease bundleRelease
   ```
3. ستجد المخرجات في:
   - `android/app/build/outputs/apk/release/app-release.apk`
   - `android/app/build/outputs/bundle/release/app-release.aab`

---

## 🔑 مفتاح التوقيع (Keystore)
- التطبيق مضبوط ومجهز للتوقيع التلقائي باستخدام المفتاح `android.keystore` (الاسم المستعار: `yawmiyati`) — غير مرفوع إلى GitHub، ويبقى على جهازك فقط.
- كلمة المرور لا تُكتب في الكود إطلاقاً. Gradle يقرأها من:
  1. متغيّر البيئة `KEYSTORE_PASSWORD` (هكذا يعمل بناء GitHub Actions، عبر Secrets المُعدّة مسبقاً: `KEYSTORE_B64` و`KEYSTORE_PASSWORD`)، أو
  2. ملف `android/keystore.properties` (غير مرفوع لـ Git) — انسخه من `keystore.properties.example` وضع فيه كلمة المرور الفعلية للبناء المحلي.
- بدون أحد المصدرين، البناء يتوقف برسالة خطأ واضحة بدل استخدام كلمة افتراضية.
