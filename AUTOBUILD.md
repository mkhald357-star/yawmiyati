# بناء APK تلقائياً — بلا تثبيت أي أداة على جهازك

## الفكرة

خوادم GitHub تملك Android SDK وJava وكل ما يلزم. نضع فيها وصفة البناء،
فتبني لك التطبيق وتسلّمك ملف APK موقّعاً. أنت لا تثبّت شيئاً، وأنا لا أحتاج
شبكة. اضغط زراً واحداً كلما أردت نسخة جديدة.

## خطوات لمرّة واحدة

**١) ارفع ملفات التطبيق** إلى مستودع باسم `yawmiyati` كما في README،
وفعّل GitHub Pages. تأكّد أن الرابط يعمل:
`https://mkhald357.github.io/yawmiyati/`

**٢) ارفع ملفين إضافيين في جذر المستودع:**

- `twa-manifest.json` (المرفق — عدّل الروابط إن اختلف اسم المستودع أو حسابك)
- `.github/workflows/build-apk.yml` (المرفق)

> لإنشاء المسار على الموقع: Add file ← Create new file ← اكتب في خانة الاسم
> `.github/workflows/build-apk.yml` وستُنشأ المجلّدات وحدها، ثم ألصق المحتوى.

**٣) شغّل البناء:** تبويب **Actions** ← «بناء تطبيق أندرويد» ← **Run workflow**.

بعد ٥–٨ دقائق تجد في أسفل صفحة التشغيل قسم **Artifacts** فيه:

| الملف | ما هو |
|---|---|
| `app-release-signed.apk` | ثبّته على هاتفك مباشرة |
| `app-release-bundle.aab` | لرفعه على Google Play |
| `keystore-base64.txt` | **مفتاح التوقيع — احفظه فوراً** |
| `fingerprint.txt` | بصمة SHA256 لملف assetlinks |

## خطوة حرجة بعد أول بناء

في أول تشغيل يُنشأ **مفتاح توقيع جديد**. إن ضاع، لن تستطيع تحديث التطبيق
لاحقاً بنفس الهوية وسيضطر هاتفك لحذف النسخة القديمة أولاً. لذلك:

1. نزّل `keystore-base64.txt` وافتحه وانسخ محتواه كاملاً.
2. Settings ← Secrets and variables ← Actions ← **New repository secret**
3. الاسم: `KEYSTORE_B64` — القيمة: ما نسخته.
4. أضف سرّاً ثانياً `KEYSTORE_PASSWORD` بكلمة سر تختارها (إن تركته سيستخدم
   كلمة افتراضية — غيّرها).

بعدها كل بناء يستخدم نفس المفتاح، والتحديثات تُثبَّت فوق القديمة بسلاسة.

## إزالة شريط المتصفّح من أعلى التطبيق

افتح `fingerprint.txt` وخذ قيمة **SHA256**، وضعها في
`wellknown/assetlinks.json` مكان `ضع_هنا_بصمة_مفتاح_التوقيع`، ثم ارفع الملف
إلى مستودع باسم `mkhald357.github.io` داخل مجلّد `.well-known/` ليصير رابطه:

```
https://mkhald357.github.io/.well-known/assetlinks.json
```

بدون هذه الخطوة يعمل التطبيق لكن يظهر شريط عنوان رفيع في أعلاه.

## للتحديث لاحقاً

عدّل رقمي `appVersionName` و`appVersionCode` في `twa-manifest.json`
(زِد الرقم الثاني بواحد على الأقل)، احفظ، وسيبدأ البناء وحده.

## صراحة أخيرة

لم أستطع تجربة هذا السير على خوادم Google لأن منافذها محجوبة عندي، فتحقّقتُ
مما أملك التحقّق منه: صحّة صياغة الملف، وسلامة بنيته، ووجود كل خطوة لازمة.
إن تعثّرت خطوة عندك، سجلّ التشغيل في Actions يُظهر السطر بالضبط — أرسله لي
وأصلحه في دقيقة.
