# فحص يدوي سريع قبل كل نشر

لا يوجد اختبارات آلية (التطبيق ملف واحد شخصي، وإضافة Playwright/Jest له
تكلفة أكبر من فائدتها حالياً). بدلاً من ذلك: شغّل هذه القائمة يدوياً (٥
دقائق) بعد أي تعديل في `index.html`، وقبل رفع نسخة جديدة.

## الأساسيات
- [ ] يفتح التطبيق بدون شاشة بيضاء أو خطأ في الكونسول (F12 ← Console)
- [ ] إضافة حركة مالية (دخل/مصروف) تظهر فوراً في الرصيد والتقارير
- [ ] إضافة مهمة، تحديدها كمنجزة، ثم حذفها (والتراجع Undo إن ظهر)
- [ ] بدء مؤقّت وقت (نشاط) وإيقافه، والتأكد من تسجيله في السجل
- [ ] إضافة دين وتسويته، والتأكد من انعكاسه على الرصيد إن كان خيار
      "تحريك المبلغ" مفعّلاً

## المنبّه (أكثر جزء حسّاس)
- [ ] ضبط منبّه على دقيقة قادمة، وقفل الشاشة، والتأكد من وصول الإشعار
- [ ] فتح الإشعار يفتح شاشة تحدي الحساب ويطلب حلّه لإيقاف الصوت
- [ ] تجربة "منبّه فائت" — أغلق التطبيق تماماً بعد موعد منبّه، ثم افتحه
      وتأكّد أنه يرنّ بدل أن يمرّ بصمت

## العمل بلا إنترنت
- [ ] فعّل وضع الطائرة، أغلق التطبيق وأعد فتحه — يجب أن يعمل بلا مشاكل
- [ ] بعد رفع نسخة جديدة، تأكد من زيادة رقم `CACHE` في `sw.js`
      (وإلا ستبقى الأجهزة على النسخة القديمة المخزَّنة)

## البيانات
- [ ] نسخة احتياطية (تصدير) ثم استعادة (استيراد) على نفس الجهاز تُرجع
      البيانات كما هي بلا فقدان
- [ ] الإعدادات ← "سجل الأخطاء" فارغ أو معقول — لا رسائل متكررة غريبة

## أندرويد (APK)
- [ ] بعد بناء APK جديد عبر GitHub Actions، ثبّته فوق النسخة القديمة على
      هاتف حقيقي (ليس فقط محاكي) وتأكد أنه يحدّث بلا حذف البيانات
- [ ] زر الرجوع الفيزيائي يتصرف بشكل صحيح (يغلق النوافذ المنبثقة أولاً)
- [ ] الاختصارات السريعة (ضغط مطوّل على الأيقونة) تفتح الشاشة الصحيحة
